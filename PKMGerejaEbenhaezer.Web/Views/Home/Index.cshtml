@using PKMGerejaEbenhaezer.Web.Models.Home
@model IndexVM
@{
    ViewData["Title"] = "Home Page";
    var cultureInfo = new System.Globalization.CultureInfo("id-ID");
}


<!-- Hero Section -->
<section id="hero">
    <h1 data-aos="fade-up">Selamat Datang</h1>
</section>
<!-- Akhir Hero Section -->


<!-- Sejarah Section -->
<section id="sejarah">
    <div class="row-container">
        <div class="content-sej">
            <h2>Sejarah GMIT Ebenhaezer Oeba Kupang</h2>
            <p>
                Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
                Gravida in fermentum et sollicitudin ac orci phasellus egestas. Vestibulum morbi blandit cursus risus at. At in tellus integer
                feugiat.
            </p>
            <p>
                Turpis massa tincidunt dui ut ornare lectus sit.
                Sagittis id consectetur purus ut. Dignissim diam quis enim lobortis. Amet est placerat in egestas erat imperdiet
                sed euismod nisi. At imperdiet dui accumsan sit. Imperdiet nulla malesuada pellentesque elit eget gravida cum sociis natoque.
                Urna cursus eget nunc scelerisque viverra mauris in aliquam sem.
            </p>
        </div>
        <div class="image-container">
            <img src="~/img/background_gereja_testing.jpg" alt="Gereja Image" />
        </div>
    </div>
</section>
<!-- Akhir Sejarah Section -->


<!-- Pendeta Section -->
<section id="pendeta">
    <h1>Pendeta GMIT Ebenhaezer Oeba</h1>
    <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit aliqua.</p>
    <div class="pendeta-box">
        <div class="owl-carousel owl-theme fernand">

            @foreach (var pendeta in Model.DaftarPendeta)
            {
                <div class="profile">
                    <div class="gambar-pen">
                        @if (pendeta.Foto is not null)
                        {
                            <picture>
                                <source media="(max-width: 465px)"
                                        srcset="@Url.Action("FotoKompresi", "Foto", new {Area = "", id = pendeta.Foto.Id})" />
                                <img src="@Url.Action("Index", "Foto", new {Area = "", id = pendeta.Foto.Id})" alt="" />
                            </picture>
                        }
                        else
                        {
                            <img src="~/img/generaluser.png" alt="" />
                        }
                    </div>
                    <h6>@pendeta.Nama</h6>
                    <p>Pendeta</p>
                    <div class="pen-link">
                        <a @(pendeta.FacebookProfileLink is not null ?
                            $"href={pendeta.FacebookProfileLink.AbsoluteUri}" : "class=disabled")>
                            <i class="ri-facebook-fill"></i>
                        </a>

                        <a @(pendeta.InstagramProfileLink is not null ?
                            $"href={pendeta.InstagramProfileLink.AbsoluteUri}" :  "class=disabled")>
                            <i class="ri-instagram-fill"></i>
                        </a>

                        <a @(pendeta.YoutubeProfileLink is not null ?
                            $"href={pendeta.YoutubeProfileLink.AbsoluteUri}" : "class=disabled")>
                            <i class="ri-youtube-fill"></i>
                        </a>
                    </div>
                </div>
            }
        </div>
    </div>
</section>
<!-- Akhir Pendeta Section -->


<!-- Injil Section -->
<section id="injil">
    <div class="injil-text">
        <h6 class="mt-3">
            "Karena tidak ada seorangpun yang dapat meletakkan dasar lain dari pada dasar yang telah diletakkan, yaitu Yesus Kristus.
            Entahkah orang membangun di atas dasar ini dengan emas, perak, batu permata, kayu, rumput kering atau jerami,
            sekali kelak pekerjaan masing-masing orang akan nampak. Karena hari Tuhan akan menyatakannya, sebab ia akan nampak
            dengan api dan bagaimana pekerjaan masing-masing orang akan diuji oleh api itu."
        </h6>
        <p class="mt-4">( 1 Korintus 3:11-13 )</p>
    </div>
</section>
<!-- Akhir Injil Section -->


<!-- Char Jemaat Section -->
<section id="about-container">
    <!-- Char -->
    <div class="about-char">
        @{
            var chartModel = new ChartModel
            {
                Type = ChartType.Doughnut,
                Label = "Jemaat Gereja",
                Data = new Dictionary<string, double>
                {
                    {"Lansia", Model.TotalLansia},
                    {"Dewasa", Model.TotalDewasa},
                    {"Pemuda", Model.TotalPemuda},
                    {"Remaja", Model.TotalRemaja},
                    {"Anak", Model.TotalAnak},
                },
            };
        }

        <vc:chart chart-model=chartModel></vc:chart>
    </div>
    <!-- Akhir Char -->
    <div class="about-text">
        <h2>Jumlah Jemaat GMIT Ebenhaezer Oeba</h2>
        <p>
            Total Jemaat : @(Model.TotalAnak + Model.TotalRemaja + Model.TotalPemuda + Model.TotalDewasa + Model.TotalLansia)
        </p>
        <div class="counters">
            <div>
                <div class="=counter">
                    <h3><span data-count="@Model.TotalLansia">0</span></h3>
                    <h4>Lansia</h4>
                </div>
                <div class="=counter">
                    <h3><span data-count="@Model.TotalDewasa">0</span></h3>
                    <h4>Dewasa</h4>
                </div>
                <div class="=counter">
                    <h3><span data-count="@Model.TotalPemuda">0</span></h3>
                    <h4>Pemuda</h4>
                </div>
                <div class="=counter">
                    <h3><span data-count="@Model.TotalRemaja">0</span></h3>
                    <h4>Remaja</h4>
                </div>
                <div class="=counter">
                    <h3><span data-count="@Model.TotalAnak">0</span></h3>
                    <h4>Anak</h4>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Akhir Chart Section -->


<!-- Warta Section -->
<section class="warta">
    <!-- Header Warta Section -->
    <header class="section-header">
        <div class="header-text">
            <h1>Warta Jemaat</h1>
            <p>
                Lorem ipsum dolor sit amet, consectetur adipiscing elit,
                sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
            </p>
        </div>
        <a asp-action="Index" asp-controller="WartaJemaat" class="warta-btn btn">Lihat Semua</a>
    </header>
    <!-- Akhir Header Warta Section -->
    <!-- Content Warta Section -->
    <div class="warta-contents">
        <div class="owl-carousel owl-theme fajar">
            @foreach (var warta in Model.DaftarWartaJemaat)
            {
                <!-- Ini Cardnya -->
                <div class="item warta-card">
                    <div class="image-wrapper">
                        <img src="~/img/default4_warta_jemaat.png" />
                        <div class="tanggal">
                            <div class="tanggal-text mb-1">
                                <h5></h5>
                                <h3>@warta.TanggalWarta.Day</h3>
                            </div>
                            <h4>@warta.TanggalWarta.ToString("MMMM, yyyy", cultureInfo)</h4>
                        </div>
                    </div>
                    <a href="@warta.DocumentLink.AbsoluteUri" class="tombolbaca">Baca Warta Jemaat</a>
                </div>
                <!-- Ini Akhir Cardnya -->
            }
        </div>
    </div>
    <!-- Akhir Content Warta Section -->
</section>
<!-- Akhir Warta Section -->


<!-- Ibadah Section -->
<section class="ibadah">
    <!-- Header Ibadah Section -->
    <header class="section-header">
        <div class="header-text">
            <h1>Ibadah Mingguan</h1>
            <p>
                Lorem ipsum dolor sit amet, consectetur adipiscing elit,
                sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
            </p>
        </div>
        <a asp-action="Index" asp-controller="Ibadah" class="ibadah-btn btn">Lihat Semua</a>
    </header>
    <!-- Akhir Header Ibadah Section -->
    <!-- Content Ibadah Section -->
    <div class="ibadah-contents">
        <div class="owl-carousel owl-theme fajar">

            @foreach (var ibadah in Model.DaftarIbadah)
            {
                <!-- Awal Card -->
                <div class="ibadah-card">
                    <div class="nats">
                        @if (ibadah.Renungan is not null)
                        {
                            <h2>"@ibadah.Renungan"</h2>
                        }
                        else
                        {
                            <h2>"@ibadah.NasPembimbing"</h2>
                        }
                    </div>
                    <div class="info-atas">
                        <div class="date-kiri">
                            <div style="text-align:center; display: flex">
                                <h2><i class="ri-map-pin-range-line" style="padding-right: 5px;"></i> @ibadah.Tempat</h2>
                            </div>
                        </div>
                        <div class="date-kanan">
                            <div style="text-align:center; display:flex;">

                                <div style="display:flex; flex-direction:column;">
                                    <h2><i class="ri-calendar-schedule-line" style="padding-right:5px;"></i> @ibadah.TanggalIbadah.ToString(cultureInfo.DateTimeFormat.LongDatePattern, cultureInfo)</h2>
                                    <h2>
                                        <i class="ri-time-line" style="padding-right: 5px;"></i>
                                        @ibadah.TanggalIbadah.ToString("hh.mm tt")
                                        @if (ibadah.KategoriIbadah is not null)
                                        {
                                            @($"- {ibadah.TanggalIbadah.Add(ibadah.KategoriIbadah.Durasi).ToString("hh.mm tt")}")
                                        }
                                    </h2>
                                </div>

                            </div>
                        </div>
                    </div>
                    <img src="~/img/Ibadah Minggu9.png" />
                    <div class="category-pendeta-ibadah">
                        <div class="subject-ibadah"
                             style="background-color: rgb(@ibadah.KategoriIbadah?.Warna.R, @ibadah.KategoriIbadah?.Warna.G, @ibadah.KategoriIbadah?.Warna.B)">
                            <h3>@ibadah.KategoriIbadah?.Nama</h3>
                        </div>
                        <div class="gambar-pendeta">
                            @if (ibadah.Pendeta?.Foto is not null)
                            {
                                <picture>
                                    <source media="(max-width: 465px)"
                                            srcset="@Url.Action("FotoKompresi", "Foto", new {Area = "", id = ibadah.Pendeta.Foto.Id})" />
                                    <img src="@Url.Action("Index", "Foto", new {Area = "", id = ibadah.Pendeta.Foto.Id})" alt="" />
                                </picture>
                            }
                            else
                            {
                                <img src="~/img/pendeta-test.jpg" />
                            }
                            <div class="tooltip-pendeta">
                                <h3>@ibadah.Pendeta?.Nama</h3>
                            </div>
                        </div>
                    </div>
                    <h2 class="ibadah-title">
                        @ibadah.Judul
                    </h2>
                    <p>@ibadah.Deskripsi</p>

                    <div class="ibadah-info">
                        <a asp-action="Detail" asp-controller="Ibadah" asp-area="" asp-route-id="@ibadah.Id"
                            class="btn" style="width:100%;">Baca Selengkapnya...</a>
                    </div>
                </div>
                <!-- Akhir Card -->
            }
        </div>
    </div>
    <!-- Akhir Content Ibadah Section -->
</section>
<!-- Akhir Ibadah Section -->


<!-- Pengumuman Section -->
<section class="peng">
    <!-- Header Pengumuman Section -->
    <header class="section-header">
        <div class="header-text">
            <h1>Pengumuman Gereja</h1>
            <p>
                Lorem ipsum dolor sit amet, consectetur adipiscing elit,
                sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
            </p>
        </div>
        <a asp-action="Index" asp-controller="Pengumuman" class="peng-btn btn">Lihat Semua</a>
    </header>
    <!-- Akhir Header Pengumuman Section -->
    <!-- Content Pengumuman Section -->
    <div class="peng-contents">
        <div class="owl-carousel owl-theme fajar">
            @if (Model.DaftarPengumuman != null || Model.DaftarPengumuman!.Count > 0)
            {
                @foreach (var pengumuman in Model.DaftarPengumuman.Take(3))
                {
                    <!-- Ini Cardnya -->
                    <div class="item peng-card">
                        @if (pengumuman.Foto != null)
                        {
                            <picture>
                                <source media="(max-width: 465px)"
                                        srcset="@Url.Action("FotoKompresi", "Foto", new {Area = "", id = pengumuman.Foto.Id})" />
                                <img src="@Url.Action("Index", "Foto", new {Area = "", id = pengumuman.Foto.Id})" alt="" />
                            </picture>
                        }
                        else
                        {
                            <img src="~/img/gereja1.jpg" />
                        }
                        <h2 class="peng-title">@pengumuman.Judul</h2>
                        <p>@pengumuman.Isi</p>
                        <div class="peng-tanggal">
                            <span><i class="ri-calendar-fill"></i>@pengumuman.TanggalDiBuat.ToString(cultureInfo.DateTimeFormat.LongDatePattern, cultureInfo)</span>
                            <a asp-action="Detail" asp-controller="Pengumuman" asp-route-id="@pengumuman.Id" asp-area=""
                               class="tombolseleng">Baca Selengkapnya...</a>
                        </div>
                    </div>
                    <!-- Ini Akhir Cardnya -->
                }
            }
        </div>
    </div>
    <!-- Akhir Content Pengumuman Section -->
</section>
<!-- Akhir Pengumuman Section -->


<!-- Script JS -->
@section Scripts {

    <script type="text/javascript" src="~/js/counter.js"></script>
    <script type="text/javascript">
        $(function () {
            $('.badan-utama').css({
                'padding-left': '0',
                'padding-right': '0',
                'padding-top': '0rem',
            });
        });
    </script>

}