@using System.Globalization
@model List<Ibadah>
@{
    ViewData["Title"] = "Ibadah";
    var cultureInfo = new CultureInfo("id-ID");
}


<div class="ibadahh">


    <!-- Hyperlink -->
    <h1 class="title">Ibadah Mingguan</h1>
    <ul class="breadcrumbs mb-4">
        <li><a>Ibadah & Kegiatan</a></li>
        <li class="divider">/</li>
        <li><a class="active">Ibadah Mingguan</a></li>
    </ul>
    <!-- Akhir Hyperlink -->


    <!-- Tombol Tambah Ibadah -->
    <div class="mb-4 mt-4">
        <a style="width:320px;" class="btn" asp-area="Dashboard" asp-controller="Ibadah" asp-action="Tambah"> + Tambah Ibadah Mingguan</a>
    </div>
    <!-- Akhir Tombol Tambah Ibadah -->


    <!-- Container Tabel Daftar -->
    <div class="container">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Judul</th>
                    <th>Deskripsi</th>
                    <th>Kategori Ibadah</th>
                    <th>Nats Pembimbing</th>
                    <th>Isi Nats Pembimbing</th>
                    <th>Bacaan</th>
                    <th>Isi Bacaan</th>
                    <th>Tempat</th>
                    <th>Tanggal Ibadah</th>
                    <th>Jam Mulai</th>
                    <th>Jam Selesai</th>
                    <th>Pendeta</th>
                    <th>Aksi</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr style="@(item.KategoriIbadah is null || item.Pendeta is null ? "background-color:red" : "")">
                        <td>@item.Id</td>
                        <td>@item.Judul</td>
                        <td>@Html.Raw(item.Deskripsi)</td>
                        <td>@(item.KategoriIbadah is null ? "Tidak Ada" : item.KategoriIbadah.Nama)</td>
                        <td>@item.NatsPembimbing</td>
                        <td>@Html.Raw(item.IsiNatsPembimbing)</td>
                        <td>@(item.Bacaan is null ? "Tidak Ada" : item.Bacaan)</td>
                        <td>@(item.IsiBacaan is null ? "Tidak Ada" : Html.Raw(item.IsiBacaan))</td>
                        <td>@item.Tempat</td>
                        <td>@item.TanggalIbadah.ToString(cultureInfo.DateTimeFormat.LongDatePattern, cultureInfo)</td>
                        <td>@item.TanggalIbadah.ToString("hh:mm tt")</td>
                        <td>
                            @if (item.KategoriIbadah is not null)
                            {
                                @item.TanggalIbadah.Add(item.KategoriIbadah.Durasi).ToString("hh:mm tt")    
                            }
                        </td>
                        <td>@(item.Pendeta is null ? "Tidak Ada" : item.Pendeta.Nama)</td>
                        <td>
                            <div class="d-flex gap-1 justify-content-start align-items-center">
                                <a class="btn" asp-action="Edit" asp-route-id="@item.Id">
                                    <i class="fas fa-pen-square"></i>
                                </a>
                                <!-- Button trigger modal -->
                                <a class="btndel" data-bs-toggle="modal" data-bs-target="#exampleModal@(item.Id)">
                                    <i class="fa fa-regular fa-trash"></i>
                                </a>

                                @{
                                    var modalVM = new DeleteConfirmationModalVM()
                {
                    ModalId = $"exampleModal{item.Id}",
                    ModalTitle = "Hapus Ibadah",
                    FormAction = Url.Action("Hapus", "Ibadah", new { Area = "Dashboard", id = item.Id })!,
                };
                                }

                                <partial name="_DeleteConfirmationModalPartial" model="modalVM" />
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <!-- Akhir Container Tabel Daftar -->


</div>


<!-- Script JS -->
@section Scripts{
    <partial name="_DataTablePartial" />
}